@using NguyenVietTien.SachOnline.Models

<!-- NAVBAR -->
<nav class="navbar navbar-inverse">
    <!-- navbar-fixed-top -->
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar1" aria-expanded="false">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="@Url.Action("Index", "NguyenVietTien_SachOnline")">SachOnline</a>
        </div>

        <div class="collapse navbar-collapse" id="navbar1">
            <!-- Menu trái -->
            <ul class="nav navbar-nav">
                <li class="active"><a href="@Url.Action("Index", "NguyenVietTien_SachOnline")">TRANG CHỦ</a></li>
                <li><a href="#">GIỚI THIỆU</a></li>
                <li><a href="#">LIÊN HỆ</a></li>
            </ul>

            <!-- Menu phải -->
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    @if (Session["TaiKhoan"] != null)
                    {
                        KHACHHANG khachHang = (KHACHHANG)Session["TaiKhoan"];
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" aria-haspopup="true">
                            Xin chào: @khachHang.HoTen <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a href="#"><span class="glyphicon glyphicon-user"></span> Thông tin cá nhân</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="@Url.Action("DangXuat", "NguyenVietTien_User")"><span class="glyphicon glyphicon-log-in"></span> Đăng xuất</a></li>
                        </ul>
                    }
                    else
                    {
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" aria-haspopup="true">
                            Người dùng <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a href="@Url.Action("DangKy", "NguyenVietTien_User")"><span class="glyphicon glyphicon-user"></span> Đăng ký</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="@Url.Action("DangNhap", "NguyenVietTien_User")"><span class="glyphicon glyphicon-log-in"></span> Đăng nhập</a></li>
                        </ul>
                    }
                </li>
                <li><a href="#"><span class="glyphicon glyphicon-shopping-cart"></span> Giỏ hàng (0)</a></li>
            </ul>

            <!-- Tìm kiếm -->
            <form class="navbar-form navbar-right" role="search">
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="Tìm kiếm">
                </div>
                <button type="submit" class="btn btn-default">Tìm</button>
            </form>
        </div>
    </div>
</nav>
